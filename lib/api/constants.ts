/**
 * Shared by the client hooks and the server readers. It lives on its own so a
 * Server Component can import it without pulling the React Query layer in.
 */
export const COMPANIES_PAGE_SIZE = 20;
